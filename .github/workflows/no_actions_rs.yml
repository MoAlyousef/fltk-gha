name: no-actions-rs

on:
  push:

defaults:
  run:
    shell: bash

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build binary
        run: |
          cargo build --target=windows-pc-stable-gnu --verbose --release
